language: python
python: 3.4
env:
  globals:
    - DJANGO_SETTINGS_MODULE="edwin.settings"
    - DJANGO_CONFIGURATION="Base"
install:
  - npm --python=$(which python2.7) install
script:
  - ./manage.py test
  - npm test
