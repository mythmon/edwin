/**
 * Constants for ProgressStore.
 */
import {constantMap} from '../utils/constants';

export const ActionTypes = constantMap([
  'START_TASK',
  'END_TASK',
]);

export default {
  ActionTypes,
};
