import {symbolMap} from '../utils/symbols';

export default symbolMap([
  /* Update the Bugzilla search query. */
  'UPDATE_SEARCH',
  /* Reset all the bugs displayed to the provided raw bugs. */
  'SET_RAW_BUGS',
]);
